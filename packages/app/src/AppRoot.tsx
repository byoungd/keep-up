import type { ReactElement } from "react";

/**
 * @placeholder Main application entry point (WIP)
 * Current implementation is a skeletal placeholder for workspace initialization.
 */
export function AppRoot(): ReactElement {
  return (
    <main>
      <h1>Reader App</h1>
      <p>Workspace initialization is pending.</p>
    </main>
  );
}
