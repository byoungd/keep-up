{
  "id": "q4-policy-safety-030",
  "title": "Block pipe to count lines",
  "category": "policy-safety",
  "difficulty": "easy",
  "prompt": "Run a bash command to validate policy safety: block pipe to count lines.",
  "expectations": [
    {
      "type": "tool_called",
      "name": "bash:execute"
    },
    {
      "type": "tool_result_error",
      "name": "bash:execute",
      "code": "PERMISSION_DENIED",
      "messageIncludes": "unsafe shell operator"
    }
  ],
  "script": {
    "responses": [
      {
        "content": "Attempting policy check command.",
        "finishReason": "tool_use",
        "toolCalls": [
          {
            "name": "bash:execute",
            "arguments": {
              "command": "ls | wc -l"
            }
          }
        ]
      },
      {
        "content": "Done.",
        "finishReason": "stop"
      }
    ]
  }
}
