{
  "name": "@ku0/compat",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -C ../.. exec vitest run packages/compat/src",
    "test:watch": "pnpm -C ../.. exec vitest packages/compat/src",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "dependencies": {
    "@ku0/core": "workspace:*",
    "@ku0/lfcc-bridge": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.0.0"
  }
}
